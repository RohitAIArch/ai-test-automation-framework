<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<!-- Proxy configuration -->


	<modelVersion>4.0.0</modelVersion>

	<!-- 🔹 Update with your org -->
	<groupId>com.yourcompany</groupId>
	<artifactId>ai-test-automation-framework</artifactId>
	<version>1.0.0</version>
	<packaging>jar</packaging>

	<name>AI Test Automation Framework</name>
	<description>Custom JUnit 5 based automation framework with JSON reporting</description>

	<properties>
		<java.version>17</java.version>
		<junit.version>5.10.0</junit.version>
		<jackson.version>2.17.0</jackson.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.junit.platform</groupId>
			<artifactId>junit-platform-commons</artifactId>
			<version>1.10.0</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.junit.platform</groupId>
			<artifactId>junit-platform-engine</artifactId>
			<version>1.10.0</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.junit.platform</groupId>
			<artifactId>junit-platform-launcher</artifactId>
			<version>1.10.0</version>
			<scope>test</scope>
		</dependency>
		<!-- ✅ JUnit 5 (Extension API) -->
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter-api</artifactId>
			<version>${junit.version}</version>
		</dependency>

		<!-- ✅ Needed if running tests inside framework (optional) -->


		<!-- ✅ JSON Processing -->
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
			<version>${jackson.version}</version>
		</dependency>

	</dependencies>
	<repositories>
		<repository>
			<id>central</id>
			<url>https://repo.maven.apache.org/maven2</url>
		</repository>
	</repositories>
	<build>

		<plugins>


			<!-- ✅ Compiler Plugin -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.11.0</version>
				<configuration>
					<source>${java.version}</source>
					<target>${java.version}</target>
				</configuration>
			</plugin>

			<!-- ✅ Surefire Plugin (JUnit 5 Support) 
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>3.2.5</version>
				<configuration>
					<useModulePath>false</useModulePath>
				</configuration>
			</plugin>
			-->
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
				<version>3.1.0</version>
				<configuration>
					<mainClass>
						com.yourcompany.automation.framework.runner.JsonTestRunner</mainClass>
					<classpathScope>test</classpathScope>
					<arguments>
						<argument>-cp</argument>
						<argument>
							${project.build.outputDirectory};
							${project.basedir}/libs/junit-platform-commons-1.10.0.jar;
							${project.basedir}/libs/junit-platform-engine-1.10.0.jar;
							${project.basedir}/libs/junit-platform-launcher-1.10.0.jar
						</argument>
					</arguments>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>